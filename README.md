# Framelog
### Work in progress

## Intro
Frame is destktop application that runs in the background on the user's computer. It will capture a screenshot of the active windows every 60 seconds and extract informations about this window, name, URL, objects, text, etc. information is stored in an encrypted database. The image will be converted to webp to keep it quality almost lossless and size small.